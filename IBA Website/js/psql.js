
/*
const { Client } = require('pg');

const client = new Client({
    user: 'simox',
    host: 'localhost',
    database: 'ibacocktails',
    password: 'simox',
    port: 5432,
});

function queryAll() {
    client.query("select * from cocktail;", (err, res) => {
        if (err) {
            console.log(err.stack)
        } else {
            console.log(res.rows)
        }
    });
}
*/
